# Local-only default
# This client uses CRA `proxy` in `client/package.json` to reach the backend at http://localhost:5000.
#
# If you ever deploy again, you can add:
# REACT_APP_API_BASE_URL=https://your-backend-domain

